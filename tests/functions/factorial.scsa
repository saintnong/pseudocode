# Recursive function test
# also test function serialisation

FUNCTION factorial(n)
    IF n < 2 THEN
        RETURN 1
    END IF
    sub = n - 1
    rec = factorial(sub)
    RETURN n * rec
END factorial

f = factorial(10)
PRINT(f)
# EXPECT: 3628800
PRINT(factorial)
# EXPECT: <FUNCTION factorial>
